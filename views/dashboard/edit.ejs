<div class="container">
    <%- include('dashheader') %>
    <form action="/dashboard/updateUserinfo" method="POST">
        <div class="form-group">
            <label for="name">Name</label>
            <input class="form-control" type="text" name="name" required value="<%= userData.name %>">
        </div>
        <div class="form-group">
            <input class="form-check-label" type="checkbox" name="private" value="on" <% if (private) { %> checked <% } %>>
            <label for="private">Privat</label>
        </div>
        <button type="submit" class="btn btn-success">Ändern</button>
    </form>
    <p>Api-Token: <%= userData.apiToken %></p>
</div>